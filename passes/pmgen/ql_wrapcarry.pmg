pattern ql_wrapcarry

match carry
	select carry->type.in(\carry_follower)
endmatch

match lut
	select lut->type.in(\LUT4)
	index <SigSpec> port(lut, \I0) === port(carry, \A)
	index <SigSpec> port(lut, \I1) === port(carry, \B)
endmatch

code
	accept;
endcode
